[project]
name = "cosmic-lab-api"
version = "0.1.0"
description = "A small API for practicing HTTP requests"
requires-python = ">=3.13.2"

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk",
]
